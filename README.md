# Etch a Sketch 

The project consits of placing a 16x16 grid of divs on the screen.
the divs should detect mouseover events, and leave a trail of pixels like a pen would.

The user has the option of pressing a button on the screen that will send a pop-up where they can enter
a positive integer (no more than 100) to create a new grid of divs in the same space.